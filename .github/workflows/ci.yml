name: CI
on: [push, pull_request]

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Verify Specs Exists
        run: |
          ls -la spec/*.schema.json
          echo "Minimal specs present."
